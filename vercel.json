{
  "name": "nexuschain",
  "builds": [{ "src": "src/**/*", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "src/cli.js" }],
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "regions": ["iad1", "sfo1", "fra1"],
  "functions": {
    "src/dist/cli.js": {
      "memory": 512,
      "maxDuration": 30,
      "launcherType": "CLI"
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
